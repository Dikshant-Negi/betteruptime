[package]
name = "worker"
version = "0.1.0"
edition = "2024"

[dependencies]
redis = { version = "0.20.2", features = ["tls","streams"] }
redis_stream = {path="../redis_stream"}
uuid = {version = "1.18.1" ,features=["v4"]}
tokio = {version = "1.48.0" , features = ["full"]}
reqwest = { version = "0.11", features = ["json"] }
futures = "0.3.31"
